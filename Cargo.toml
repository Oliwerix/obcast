[workspace]
resolver = "2"
members = [
    "crates/obcast-proto",
    "crates/obcast-server",
    "crates/obcast-client",
]

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
